# 진행 노트 — 주간 변화 지표 계산 함수

## 태스크 정체 (DB에서 확인, agent_tasks 34246ece / project_id=4 "임원 주간 브리핑 자동화")

- expected_output: `packages/l5-core/src/functions/executive-brief.ts` 내 `calculateWeeklyDelta(currentWeek, previousWeek)`.
  반환 `{ completedTasksDelta, newBlockersCount, completionRateDelta, completionRatePercent }`.
- 완료조건: `pnpm test -- executive-brief` 통과 / 양·음수 방향 반영 / 완료율 `(completed / (completed+inProgress+blocked)) * 100` / 신규 블로커 = 이번주 − 지난주 count.
- 형제 태스크: "NocoBase agent_tasks 집계 함수 구현"(데이터 수집), "Slack 메시지 포맷팅 함수"(표현, research/spec 완료), "Slack Gateway 발송", "Hermes 주간 스케줄", "통합 테스트 및 라이브 전환".

## phase: research (2026-07-09) — 완료

- 산출물: `docs/research/weekly-delta-metric-calculation.md`
- 결정: **후보 A(순수 TS zero-dep 함수) 채택**. B(SQL window function 계산)는 "NocoBase는 Shell" 원칙 위반+단위테스트 불가로 배제, C(simple-statistics/arquero)는 산술 4개에 과잉+불필요 dep으로 배제.
- npm 실측(2026-07-09): simple-statistics 7.9.3(ISC), arquero 8.0.3(BSD-3) — 배제 근거용.
- codex QA 1회전: **PASS**. 경미한 보완 노트 1건(completionRateDelta 계산 시 current/previous 어느 한쪽 분모가 0인 경우도 spec에서 명시) → 리서치 문서 미결정 1번에 반영 완료.

## 다음 phase(spec)에서 결정할 것

1. 분모 0일 때 `completionRatePercent`: 0 vs null ("데이터 없음"과 "0%" 구분).
2. 첫 주(previousWeek 부재/전부 0): 델타 의미 정의.
3. 입력 타입 `WeeklySnapshot` 필드명 — 집계 함수 태스크와 계약 일치 필요(소스: `agent_tasks.status`+`blocker`).
4. `completionRateDelta` = 완료율 %p 차이로 명문화.
5. 반올림 정책(표시용 1자리 제안).

## 함정 인계

- 집계 태스크(형제)는 NocoBase `createdAt` camelCase 함정 주의 — `created_at` 정렬 시 조용한 빈배열.
- 주차 경계 타임존: Trigger.dev cron은 UTC(critic이 기획 단계에서 지적한 must_fix) — 집계/스케줄 태스크 소관이지만 스냅샷 주차 정의에 영향.
