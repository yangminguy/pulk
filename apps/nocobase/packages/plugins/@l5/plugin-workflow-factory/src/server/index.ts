// @l5/plugin-workflow-factory — server (hooks)
// Collections: workflow, workflow_step.
// 워크플로우 생성/검증 로직은 @l5/core 에 위임한다 (하드코딩 금지).

// TODO: import from @l5/core — 워크플로우 생성 함수
// TODO: NocoBase Plugin 클래스 상속 및 collection 등록 구현

export default {
  name: 'workflow-factory',
  // async load() { /* TODO: collection 등록, 워크플로우 생성 액션 */ },
};
