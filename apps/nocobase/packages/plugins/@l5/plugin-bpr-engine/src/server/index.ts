// @l5/plugin-bpr-engine — server (hooks)
// Collections: bpr_log, workflow_improvement_proposal.
// 개선 제안 판단 로직은 @l5/core 에 위임한다 (하드코딩 금지).

// TODO: import from @l5/core — BPR 분석 / 워크플로우 개선 제안 함수
// TODO: NocoBase Plugin 클래스 상속 및 collection 등록 구현

export default {
  name: 'bpr-engine',
  // async load() { /* TODO: collection 등록, 실행 결과 학습 -> 개선 제안 생성 */ },
};
