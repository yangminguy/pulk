---
title: "Listen to form changes Example"
description: "Extracted example from ctx.blockModel"
---

# ctx.blockModel

## Listen to form changes

```ts
ctx.blockModel?.emitter?.on?.('formValuesChange', (payload) => {
  // React to latest form values or re-render
});
```
